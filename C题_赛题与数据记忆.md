# 2026 年第七届“华数杯”大学生数学建模竞赛 C 题｜赛题与数据记忆

## 题目名称

**面向算电协同的多目标调度优化研究**

> 用途：作为后续建模、编码与论文写作的统一事实基线。本文仅整理题目正文、附件 1 的规则/字段说明及附件数据的客观汇总；不包含自行设计的模型、策略、评价建议或基准复核推导。
>
> 整理日期：2026-08-07；赛题、附件与数据位于当前工作区根目录（`附件数据/` 为原始数据目录）。
>
> 数据预处理与探索分析的实施记录、脚本和产物见：[步骤0_数据预处理与分析](步骤0_数据预处理与分析/步骤0_数据预处理与分析_记忆.md)。
>
> 问题一短期 GPU 需求预测的实施记录、代码和结果见：[步骤1_问题一_预测模型](步骤1_问题一_预测模型/步骤1_问题一_预测模型_记忆.md)。

## 1. 赛题背景（题目正文）

人工智能算力规模增长使数据中心的用电需求、碳排放和电网交互影响增加，电力成本已成为数据中心运营成本的重要组成。算电协同关注算力负荷与能源系统的实时耦合：西部拥有较丰富的风能、太阳能和水电资源，电价相对较低；东部接近用户侧，网络时延较低且实时业务需求较高。

题目要求研究计算任务、电力系统、可再生能源、储能系统和通信网络之间的多目标协同优化。将 AI 任务迁移到新能源资源丰富区域可能降低电费和碳排，但会带来更高网络时延；实时推理对时延敏感，训练任务更关注能源成本和算力规模；新能源出力具有波动性，需要储能协调。

系统设置六个典型区域：RegionA、RegionB、RegionC 为东部高负荷区域；RegionD 为西部算力中心；RegionE、RegionF 分别为光伏、风电资源富集区域。区域经广域网络相连。

## 2. 统一时间与任务规则

- 六个区域通过广域网相连：A/B/C 为东部高负荷、低时延用户侧区域；D 为西部大算力中心；E 为光伏资源富集区；F 为风电资源富集区。
- 主任务到达时域为 `t=0,…,2399`，时间粒度为 1 小时。电力与储能数据覆盖 `t=0,…,2406`。
- `t=2400,…,2405`：无新任务，仅用于结清在第 2399 小时前到达但尚未完成的任务；这些时段计入问题 2–4 的相关能源、成本和碳排放结算。
- `t=2406`：无新任务，也**不得运行任何未完成任务**；只用于电力与储能终端状态结算。因此所有任务的实际执行区间必须在时点 2406 前结束，不能占用第 2406 小时。
- 任务均为不可抢占、不可拆分、不可中途迁移的连续作业。`GPU_Demand` 在整个执行期间持续占用；小时容量需按作业在该小时内的实际重叠时长折算为 GPU-hour。

题目设置三类 AI 任务：

| TaskType | 中文名称 | 时延敏感性 | 附件 1 的调度说明 |
|---|---|---|---|
| `RealTimeInference` | 实时推理 | High | 时延约束强；到达即开工，并满足网络时延和 SLA 约束。 |
| `BatchInference` | 批量推理 | Medium | 具有一定调度弹性，在成本、时延、容量之间折中。 |
| `AITraining` | AI 训练 | Low | 对实时网络时延不敏感，可在第 2406 小时前完成。 |

## 3. 赛题问题与题目明确说明

| 问题 | 题目要求 | 题干/附件中的明确说明 |
|---|---|---|
| 1 | 分区域、分任务类型统计 GPU 需求；建立短期预测模型；给出 `2376–2399` 实际到达任务的基础调度；展示最后 24 小时调度甘特图与区域 GPU 利用率。 | 预测建议：`0–2351` 训练、`2352–2375` 调参验证、模型确定后 `0–2375` 重训、`2376–2399` 测试。最后 24 小时调度**一律使用实际到达任务**，不使用预测值。 |
| 2 | 以 `0–2399` 小时实际到达任务及对应逐时电力参数为输入，考虑区域电价、碳强度、可用新能源出力；以任务迁移与开工时段为决策变量，建立碳感知任务调度模型并给出调度策略。运行成本、碳排放、网络时延和新能源利用率为目标或评价指标。 | 迁移后的设施负荷按统一功率映射和区域 PUE 计算。 |
| 3 | 在给定任务调度和逐时负荷基础上，建立储能协同优化模型，设计储能充放电策略；分析储能对运行成本、碳排放、区域峰值净购电功率、负荷波动程度的影响。 | IT 负荷取 `Baseline_AI_IT_Load_MW` 与 `NonAI_IT_Load_MW`，设施负荷为给定 IT 负荷 × PUE；仅优化储能充放电、购售电和新能源分配，不重新优化任务迁移与任务开工时段。 |
| 4 | 在前三问基础上，建立多区域算—储—电协同优化模型，统一考虑任务迁移、电价、碳排放、可用新能源、储能、区域购售电边界、网络时延及任务异构性。 | 在系统运行成本、碳排放、网络时延、服务质量、新能源利用率和区域峰值净购电之间权衡；比较不同碳约束、电价机制和新能源波动场景下的策略变化。题目不要求建立线路潮流、网络带宽、迁移数据量、传输能耗或传输费用模型。 |

### 题干与附件 1 的数据使用说明

- 附件数据同时包含输入参数、基准运行状态和基准结果。参赛队应按各问题要求重新计算优化后的调度结果，并与附件给出的基准状态比较分析。
- 问题 2 至问题 4 为保证结果可比性，原则上使用附件给出的实际数据作为统一优化输入。
- 工作负载到达数据覆盖 `0–2399`；`region_time_data.xlsx` 覆盖 `0–2406`。第 `2376–2399` 小时的预测精度仅用于预测评价。
- 问题 1、2、4 的调度结果须满足 GPU 容量、IT 功率、设施功率、网络时延和任务完成时限；任务形成的 AI IT 功率按 `power_mapping.xlsx` 计算。

### 题目及附件明确的约束条件

1. **任务/SLA**：实时推理到达即开工，且 `NetworkLatency_ms ≤ MaxLatency_ms`；批量推理与训练可延后，但须不晚于其完成时限且所有任务均在 2406 前结束。
2. **算力与功率**：每区域、每小时同时满足 `Available_GPU`、`Max_IT_Power_MW`、`Max_Facility_Power_MW`。总 IT 功率不等于附件中的基准 `IT_Load_MW`；优化时只固定 `NonAI_IT_Load_MW`，需重新叠加调度产生的 AI IT 功率。
3. **通信**：只用单向时延矩阵筛选可行执行区域；不考虑带宽和迁移开销。
4. **储能**（问题 3/4）：SOC 上下限、充/放电功率上限、充放电效率、区域购售电上限；`SOC(2406) ≥ InitialSOC_MWh`。`InitialSOC_MWh` 是第 0 小时运行前的唯一初始状态，逐时 `SOC_MWh` 是该小时结束时的状态。
5. **电力平衡与购售电**：使用每区域 `MaxGridImport_MW`、`MaxGridExport_MW` 硬约束；本题不需要跨区域电网潮流模型。

## 4. 附件 1 给出的统一计算口径

附件 1 定义 `Overlap(i,r,t)` 为任务 i 在区域 r、小时 t 内的实际占用小时数，并给出：

```text
AI_IT_Load(r,t) = Σ_i GPU_Demand(i) × Overlap(i,r,t) × GPU_Power(TaskType_i)
IT_Load(r,t)    = NonAI_IT_Load(r,t) + AI_IT_Load(r,t)
Total_Load(r,t) = IT_Load(r,t) × PUE(r)

GridPurchase + AvailableRenewable + DischargePower
  = Total_Load + ChargePower + GridSell + Curtailment

SOC(t) = SOC(t-1) + ChargeEfficiency×ChargePower(t)
                       - DischargePower(t)/DischargeEfficiency
CarbonEmission(t) = GridPurchase(t) × CarbonIntensity(t)
ElectricityCost(t)= GridPurchase(t) × ElectricityPrice(t)
                  - GridSell(t) × SellPrice(t)
RenewableUtilization = Σ(UsedRenewable + RenewableCharge + GridSell)
                      / ΣAvailableRenewable
```

- 上述功率单位为 MW，时间步长为 1 h。
- `ChargePower = RenewableCharge + GridCharge`；`NetGridImport = GridPurchase - GridSell`。
- 题目明确：碳排按**购电功率**乘碳强度累计；新能源利用率分子包含直接消纳、可再生能源充电与外送。

## 5. 附件数据清单与浏览汇总

| 文件 | 主数据表（行×列，含表头） | 作用与使用提醒 |
|---|---:|---|
| `GPU_information.xlsx` | `GPU中心基础情况`，7×11 | 六区域 GPU、IT/设施功率、PUE 与可调度 GPU 上限；另有“字段说明”。 |
| `workload_trace.xlsx` | `Sheet1`，50,001×11 | 50,000 条真实任务到达记录，是问题 1/2/4 的任务输入。 |
| `network_latency.xlsx` | `network_latency`，37×4 | 完整 6×6 单向时延矩阵（36 条）；另含“时延矩阵”“模型说明”。 |
| `power_mapping.xlsx` | `任务功率映射`，4×3 | 三类任务等效 GPU → IT 功率的**唯一映射**。 |
| `region_time_data.xlsx` | `region_time_data`，14,443×26 | 6 区域 × 2,407 小时 = 14,442 条逐时电力、负荷、储能与基准运行状态。 |
| `storage_information.xlsx` | `storage_information`，7×13 | 六区域储能、购售电上限与 SOC 初始条件。 |

### 5.1 区域算力与设施参数

| 区域 | Total GPU | Available GPU | 最大 IT MW | PUE | 最大设施 MW | 区域定位 |
|---|---:|---:|---:|---:|---:|---|
| RegionA | 700 | 630 | 420 | 1.35 | 567.0 | 东部用户侧实时业务中心 |
| RegionB | 650 | 585 | 410 | 1.35 | 553.5 | 东部综合业务中心 |
| RegionC | 600 | 540 | 405 | 1.38 | 558.9 | 东部边缘算力节点 |
| RegionD | 1,600 | 1,472 | 720 | 1.28 | 921.6 | 西部大型算力中心 |
| RegionE | 1,100 | 1,012 | 560 | 1.25 | 700.0 | 光伏资源富集区 |
| RegionF | 1,050 | 966 | 540 | 1.27 | 685.8 | 风电资源富集区 |

预留比例：A/B/C 为 10%，D/E/F 为 8%；附件说明中 `Available_GPU` 为扣除预留比例后的可调度 GPU 容量，用于容量约束。

### 5.2 任务字段与实际数据概览

字段：`TaskID, TaskType, ArrivalHour, GPU_Demand, EstimatedDuration_min, DelaySensitivity, SourceRegion, MaxLatency_ms, LatestFinishHour, EarliestStartHour, ExecutionMode`。

- 50,000 个 `TaskID` 均唯一；无缺失值；到达小时完整落在 `0–2399`；`EarliestStartHour` 全部等于 `ArrivalHour`；任务均为 `NonPreemptive`。
- GPU 需求范围 1–127，持续时间范围 10–399 分钟。按“GPU 需求 × 时长”合计工作量为约 5,020,786.6 GPUh。

| 类型 | 任务数（占比） | GPU 需求（最小/均值/最大） | 时长均值/最大（min） | 总 GPUh | 时延阈值 | 完成时限 |
|---|---:|---:|---:|---:|---:|---|
| AITraining | 16,559（33.12%） | 16 / 71.28 / 127 | 204.5 / 399 | 4,023,538.7 | 150 ms | 2406 |
| BatchInference | 16,717（33.43%） | 4 / 13.52 / 23 | 204.2 / 399 | 769,097.8 | 80 ms | 2406 |
| RealTimeInference | 16,724（33.45%） | 1 / 4.02 / 7 | 203.9 / 399 | 228,150.1 | 20 ms | 与即时执行一致 |

- 数据中全部 16,724 条实时任务均满足：`LatestFinishHour = ArrivalHour + ceil(EstimatedDuration_min/60)`。
- 来源区域任务量/GPUh：A 10,062 / 448,452.45；B 9,200 / 430,758.15；C 7,559 / 374,934.28；D 9,560 / 1,548,858.43；E 6,912 / 1,087,748.22；F 6,707 / 1,130,035.12。三类任务的 GPUh 汇总见上表。
- 预测划分中的实际情况：训练窗 0–2351 共 48,963 任务、4,913,871.78 GPUh；验证窗 2352–2375 共 499 任务、46,928.10 GPUh；测试窗 2376–2399 共 538 任务、59,986.77 GPUh。测试窗每小时到达任务数为 16–31、均值 22.42。
- 在 `2376–2399` 到达的任务中，按“到达即开工”和时长计算，有 37 个任务的结束时点在 2400 后，2 个在 2405 后；没有任务的结束时点超过 2406。

### 5.3 网络时延数据汇总

| 源\目标 (ms) | A | B | C | D | E | F |
|---|---:|---:|---:|---:|---:|---:|
| A | 5 | 12 | 15 | 65 | 78 | 82 |
| B | 12 | 5 | 14 | 60 | 74 | 79 |
| C | 15 | 14 | 5 | 58 | 70 | 76 |
| D | 65 | 60 | 58 | 5 | 22 | 25 |
| E | 78 | 74 | 70 | 22 | 5 | 18 |
| F | 82 | 79 | 76 | 25 | 18 | 5 |

- 时延范围 5–82 ms，6×6 组合完整且无重复。
- 按数据中的 `NetworkLatency_ms ≤ MaxLatency_ms` 逐任务统计：不存在可行目标区域数为 0 的任务；训练任务均有 6 个可行区域；批量推理的可行区域数均值为 5.68（最少 5）；实时推理的均值为 2.87（最少 1、最多 3）。

### 5.4 任务功率映射

| TaskType | `GPU_Power_MW_per_EquivalentGPU` |
|---|---:|
| AITraining | 0.16 MW |
| BatchInference | 0.10 MW |
| RealTimeInference | 0.08 MW |

这是问题 1/2/4 把任务排程转换为 AI IT 负荷的唯一功率映射；不得用 `AITrainingPower_MW` 或基准 `IT_Load_MW` 代替。

### 5.5 区域逐时数据

`region_time_data.xlsx` 的索引是 `(Hour, Region)`；无缺失、无重复，小时为 0–2406。主时域 14,400 行，收尾时域 42 行。主要字段分为：

- 输入参数：`PricePeriod`（Flat/Valley/Peak）、`ElectricityPrice_CNY_per_MWh`、`SellPrice_CNY_per_MWh`、`CarbonIntensity_tCO2_per_MWh`、`AvailableRenewable_MW`。
- 基准结果/状态：`UsedRenewable_MW, RenewableCharge_MW, Curtailment_MW, IT_Load_MW, Total_Load_MW, GridPurchase_MW, GridCharge_MW, GridSell_MW, NetGridImport_MW, CarbonEmission_tCO2, SOC_MWh, ChargePower_MW, DischargePower_MW`。
- 给定负荷拆分：`Baseline_AI_IT_Load_MW` 和 `NonAI_IT_Load_MW`。前者供问题 3 固定使用；后者为问题 2/4 中不可迁移固定 IT 负荷。

| 区域 | 平均电价（元/MWh） | 平均碳强度（t/MWh） | 可再生出力（min/mean/max MW） | 基准 AI IT 均值 MW | 非 AI IT 均值 MW | 峰值净购电 MW |
|---|---:|---:|---:|---:|---:|---:|
| A | 707.45 | 0.620 | 500 / 799.80 / 1,100 | 23.31 | 314.16 | 497.00 |
| B | 677.98 | 0.580 | 500 / 799.80 / 1,100 | 22.63 | 311.07 | 467.86 |
| C | 658.33 | 0.550 | 500 / 799.80 / 1,100 | 20.03 | 312.14 | 455.27 |
| D | 422.51 | 0.420 | 500 / 799.80 / 1,100 | 98.87 | 254.18 | 282.58 |
| E | 373.38 | 0.220 | 500 / 799.80 / 1,100 | 69.51 | 283.57 | 166.06 |
| F | 393.03 | 0.260 | 500 / 799.80 / 1,100 | 72.64 | 280.70 | 152.50 |

### 5.6 储能与购售电边界

| 区域 | 容量 MWh | Min / Initial SOC MWh | 最大充/放 MW | 充/放效率 | 售电限额 MW | 最大购/售电 MW |
|---|---:|---:|---:|---:|---:|---:|
| A | 350 | 35 / 157.5 | 100 / 100 | 0.93 / 0.92 | 0 | 550 / 0 |
| B | 320 | 32 / 144.0 | 90 / 90 | 0.93 / 0.92 | 0 | 520 / 0 |
| C | 300 | 30 / 135.0 | 85 / 85 | 0.93 / 0.92 | 0 | 510 / 0 |
| D | 900 | 90 / 405.0 | 260 / 260 | 0.94 / 0.93 | 180 | 510 / 180 |
| E | 820 | 82 / 370.0 | 240 / 240 | 0.94 / 0.93 | 220 | 370 / 220 |
| F | 850 | 85 / 382.5 | 250 / 250 | 0.94 / 0.93 | 220 | 340 / 220 |

`SellLimit_MW` 与 `MaxGridExport_MW` 在现有数据中数值相同；A/B/C 的两项数值均为 0。
